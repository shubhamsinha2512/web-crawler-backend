export interface IClient {
    id: number;
    name: string;
    email: string;
    cin: string;
    pin: string;
    address: string;
}

export interface IClientDto {
    id: number;
    name: string;
    email: string;
    cin: string;
    pin: string;
    address: string;
}